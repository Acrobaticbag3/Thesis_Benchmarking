replicaCount: 4

image:
  repository: nginx
  tag: "1.0.0"
  pullPolicy: IfNotPresent

servive:
  type: ClusterIP
  port: 80

resources:
  limits:
    cpu: 200m     # min 0.2 CPU per pod
    memory: 256Mi # min memory per pod
  requests:
    cpu: 1000m    # max 1 CPU per pod
    memory: 1Gi   # max memory per pod