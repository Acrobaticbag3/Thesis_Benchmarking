apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../
patches:
  - target:
      kind: Deployment
      name: kustomize-test-app
    patch: |-
      - op: add
        path: /spec/replicaCounts
        value: 5
